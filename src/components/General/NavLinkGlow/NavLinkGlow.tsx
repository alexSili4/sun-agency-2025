import { FC } from 'react';
import linkGlow from '@/images/general/link-glow.png';
import { Image } from './NavLinkGlow.styled';

const NavLinkGlow: FC = () => {
  return <Image src={linkGlow} />;
};

export default NavLinkGlow;
