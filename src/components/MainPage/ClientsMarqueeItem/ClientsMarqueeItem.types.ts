import { Strings } from '@/types/types';

export interface IProps {
  primaryLogo: string;
  images: Strings;
  logo: string;
  projects: string;
  text: string;
  websiteAddress: string;
  years: string;
}
